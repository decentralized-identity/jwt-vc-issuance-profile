{
  "name": "dif-jwt-vc-issuance-profile",
  "version": "0.0.2",
  "description": "DIF JWT VC Issuance Profile",
  "license": "Apache 2.0",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/decentralized-identity/spec-up.git"
  },
  "scripts": {
    "render": "node -e \"require('spec-up')({ nowatch: true })\"",
    "edit": "npm run dev",
    "build": "npm run build:specs",
    "build:specs": "node -e \"require('spec-up')({ nowatch: true })\"",
    "dev": "node -e \"require('spec-up')()\"",
    "dev:specs": "node -e \"require('spec-up')()\""
  },
  "bugs": {
    "url": "https://github.com/decentralized-identity/jwt-vc-issuance-profile/issues"
  },
  "dependencies": {
    "spec-up": "^0.10.6"
  }
}
